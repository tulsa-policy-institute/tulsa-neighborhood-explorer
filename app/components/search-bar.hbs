<PowerSelect
  class="m-10 max-w-full box-border"
  @searchEnabled={{true}}
  @options={{@list}}
  @selected={{this.selected}}
  @searchField="neighborhood"
  @placeholder="Click here to type or select your neighborhood..."
  @onChange={{this.transitionTo}} as |neighborhood|
>
  {{neighborhood.neighborhood}}
</PowerSelect>
